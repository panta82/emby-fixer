const settings = require("../settings");
const logger = require("./logger");

/**
 * This is where the main business logic happens
 * @param {LibraryItem[]} libraryItems
 * @param {sqlite3.Database} db
 * @return {Promise<void>}
 */
async function fixLibrary(libraryItems, db) {}
